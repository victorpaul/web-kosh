version: 2
jobs:
  build:
    docker:
      - image: ciandt/gulp
    steps:
      - run: npm install
      - run: gulp build-loc
    artifacts:
      paths:
        - dist/
      expire_in: 1 hour
    only:
      - master